
#include<sysio.h>
//多核启动
static void mpmain()
{
        /*sys_printk("cpu%d: starting\n",cpu->id);
        idtinit();
        xchg()*/
}

//获取当前工作的cpu
struct cpu* mycpu(void)
{
return (struct cpu* )NULL;
}